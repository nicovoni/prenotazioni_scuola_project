# Django Configuration
DJANGO_SETTINGS_MODULE=backend.config.settings
DJANGO_DEBUG=False
DJANGO_SECRET_KEY=your-super-secret-key-here-minimum-50-characters
DJANGO_ALLOWED_HOSTS=your-domain.com,www.your-domain.com,localhost,127.0.0.1

# Database Configuration
# For local SQLite development (default):
# DATABASE_URL=sqlite:///db.sqlite3

# For PostgreSQL (recommended for production):
DATABASE_URL=postgresql://user:password@host:5432/database

# For MySQL:
# DATABASE_URL=mysql://user:password@host:3306/database

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp-relay.brevo.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-smtp-api-key
DEFAULT_FROM_EMAIL=your-email@example.com
ADMIN_EMAIL=admin@example.com
EMAIL_TIMEOUT=15

# School Configuration
SCHOOL_NAME=Istituto Statale di Istruzione Superiore di Follonica
AUTHOR=Sistema sviluppato da TUO NOME
SCHOOL_EMAIL_DOMAIN=example.com
ADMINS_EMAIL_LIST=admin1@example.com,admin2@example.com

# Resources Configuration
NUM_LABORATORI=5
NUM_NOTEBOOK=20
NUM_IPAD=15
CART_IPAD_TOTAL=25
CART_NOTEBOOK_TOTAL=30

# Booking Rules
BOOKING_START_HOUR=08:00
BOOKING_END_HOUR=18:00
GIORNI_ANTICIPO_PRENOTAZIONE=2
DURATA_MINIMA_PRENOTAZIONE_MINUTI=30
DURATA_MASSIMA_PRENOTAZIONE_MINUTI=180

# Cloud Provider Specific (if needed)
# For Heroku:
# PORT=8000
# For Render:
# REDIS_URL=redis://...

# For Docker deployments, additional files can be created for secrets:
# EMAIL_HOST_PASSWORD_FILE=/run/secrets/email_password
